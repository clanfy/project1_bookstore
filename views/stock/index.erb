<h2>Stock List</h2>

<% if @stock.count > 0 %>
  <ul>
  <% @stock.each do |stock| %>
  <li>
  <%= stock.quantity %> X <%= stock.book.title %> by <%= stock.book.author.name %>
  </li>
  <% end %>
  </ul>
  <% else %>
  <p> Please add stock! </p>
  <% end %>